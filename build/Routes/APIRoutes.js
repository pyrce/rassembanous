class Routes {
    static make() {
        //  Router.get("/favicon.ico", ()=>{ return null })
        //  Router.get("/", ()=>{ return HomeController.getHome() })
        // Router.get("/events", null, EventsController.getEvents )
    }
}
export default Routes;
